<div class="container">
    <h1 class="title">Human Consumption</h1>
    <div class="mouth">
        <h2>think of something to eat</h2>
        <form class="new-thing">
                <input type="text" id="something" name="name" placeholder="type something">
            <button type="submit" class="newThing btn">put it in your mouth</button>
        </form>
    </div>
    <div class="chew box">
        <ul>
            {{#each things}} 
                {{#unless digested}} 
                    {{> thing chew=true}} 
                {{/unless}} 
            {{/each}}
        </ul>
    </div>
    <div class="swallow box">
        <ul>
            {{#each things}} 
                {{#if digested}}  
                    {{> thing chew=false}} 
                {{/if}} 
            {{/each}}
        </ul>
    </div>
    <h1 id="end" class="title">THAT WAS GROSS</h1>
</div>